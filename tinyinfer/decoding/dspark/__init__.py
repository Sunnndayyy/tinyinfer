"""Paper-specific DSpark decoding components.

DSpark is deliberately a package rather than one large file because its draft
model, confidence estimates, and scheduling policy are separate mechanisms.
"""
